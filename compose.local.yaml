services:
  postgres:
    image: postgres:18-alpine3.23
    restart: on-failure
    env_file: '.env'
    ports:
      - '5432:5432'
    volumes:
      - status-line_postgres:/var/lib/postgresql

volumes:
  status-line_postgres:
